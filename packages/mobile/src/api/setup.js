// mobile/src/api/setup.js — No-op placeholder
// The mobile app uses its own api.js directly.
// Shared services are not used directly — mobile screens call the mobile api instance.
export function initMobileApi() {
  // Nothing to do — mobile screens import from '../api/api' directly
}
