// src/components/MobileNavigation.jsx
// DEPRECATED: Navigation is now provided by SiteHeader + SiteFooter via AppLayout.
// This component returns null for backward compatibility with pages that still import it.

function MobileNavigation() {
  return null;
}

export default MobileNavigation;
