# Architecture — as built

Generated by [`analyze.py`](analyze.py) from a pure `ast` parse of the source; no target code is imported or executed. Refresh with `python docs/architecture/analyze.py` from the repository root — it needs no dependencies beyond the standard library, and rewrites every file in this directory. Do not hand-edit these pages; edit the analyzer.

**Interactive map.** Open [`index.html`](index.html) in a browser — double-clicking the file is enough, no server needed. Modules are grouped into collapsible package boxes; click any node for its docstring, public interface and connections. It loads Cytoscape.js from a CDN, so the first open needs an internet connection. Full instructions: [`regenerate.md`](regenerate.md).

**Scope.** `backend/` and `ingestion/`, plus the root `run.py` entrypoint. Excluded: `alembic/` and `migrations/` (mechanical migration scripts), `backend_taste/` (a separate deployable), and `backend/venv/` (a vendored virtualenv).

**Package naming.** `backend/` is an import root, not a package — its subdirectories carry no `__init__.py` and the code imports `db.models.block`, not `backend.db.models.block`. Packages below are therefore named as the code imports them.

## At a glance

- **18 packages**, **295 modules**, **68,995 lines**
- **1343 import edges**, **329 dataflow edges**
- **44 external integration points** across 6 categories
- **0 hard circular dependencies**, 1 broken by deferred imports

## Packages

| Package | Modules | LOC | Imports | Imported by | Integrations |
| --- | --- | --- | --- | --- | --- |
| [`api.v1`](packages/api-v1.md) | 57 | 26,415 | 10 | 1 | cloud, config, db, fs |
| [`db.models`](packages/db-models.md) | 65 | 7,564 | 3 | 11 | db |
| [`scripts`](packages/scripts.md) | 36 | 7,449 | 6 | 1 | cloud, config, db, fs, http |
| [`schemas`](packages/schemas.md) | 41 | 7,051 | 1 | 2 | db |
| [`services`](packages/services.md) | 23 | 6,139 | 5 | 6 | cloud, config, db, http |
| [`ingestion.sources`](packages/ingestion-sources.md) | 10 | 3,297 | 3 | 1 | db, fs, http |
| [`scripts.data_import`](packages/scripts-data_import.md) | 9 | 2,499 | 2 | 0 | config, db, fs, http |
| [`core`](packages/core.md) | 10 | 2,430 | 4 | 10 | config, db, fs |
| [`ingestion.config`](packages/ingestion-config.md) | 9 | 2,044 | 0 | 2 | — |
| [`ingestion`](packages/ingestion.md) | 11 | 1,237 | 4 | 1 | config, db, fs |
| [`utils`](packages/utils.md) | 8 | 1,056 | 3 | 4 | cloud, config, db |
| [`app`](packages/app.md) | 4 | 816 | 5 | 0 | config, db, fs |
| [`api`](packages/api.md) | 1 | 298 | 4 | 2 | db |
| [`ingestion.scripts`](packages/ingestion-scripts.md) | 2 | 271 | 0 | 0 | fs, http |
| [`core.security`](packages/core-security.md) | 3 | 183 | 2 | 5 | db |
| [`permissions`](packages/permissions.md) | 1 | 157 | 1 | 2 | — |
| [`db`](packages/db.md) | 4 | 80 | 2 | 8 | db |
| [`backend`](packages/backend.md) | 1 | 9 | 1 | 0 | — |

## Integration surface

| Category | Targets |
| --- | --- |
| **cloud** (2) | `boto3`, `botocore` |
| **config** (3) | `dotenv`, `os.environ`, `pydantic_settings` |
| **db** (3) | `geoalchemy2`, `psycopg2`, `sqlalchemy` |
| **fs** (6) | `csv`, `io`, `json file i/o`, `open()`, `pathlib`, `shutil` |
| **http** (3) | `httpx`, `requests`, `urllib` |

### HTTP endpoints

Hosts appearing as URL literals. **Called** means the module holding the literal also imports an HTTP client, so it is a real call site. **Declared** means the literal sits in a data or config module and is handed elsewhere to be called — every weather source works this way, keeping its base URL in `config/*_sites.py` and issuing the request from `sources/*.py`. A declared host may also be an ordinary link (an app-store badge, a JSON-LD vocabulary) that is never requested at all; static analysis cannot tell those apart, so both are listed.

- **Called** (11): `data.hbrc.govt.nz`, `data.linz.govt.nz`, `datafinder.stats.govt.nz`, `envdata.tasman.govt.nz`, `gisdata.orc.govt.nz`, `hilltop.gdc.govt.nz`, `hilltop.gw.govt.nz`, `hilltop.nrc.govt.nz`, `hydro.marlborough.govt.nz`, `live.harvest.com`, `www.ncei.noaa.gov`
- **Declared** (16): `app.auxein.co.nz`, `apps.apple.com`, `auxein.co.nz`, `data.ecan.govt.nz`, `forecast-v2.metoceanapi.com`, `grow.auxein.co.nz`, `insights.auxein.co.nz`, `localhost`, `play.google.com`, `schema.org`, `static.wixstatic.com`, `taste.auxein.co.nz`, `worksafe.govt.nz`, `www.auxein.co.nz`, `www.iponz.govt.nz`, `www.ogimet.com`

## Warnings

### Circular dependencies

Counted over real edges only. Imports under `if TYPE_CHECKING:` are excluded — they never execute, so they cannot form a cycle. This matters here: the `db.models` package is full of them, and counting them would report a dozen cycles that do not exist at runtime.

**No hard cycles.** No chain of module-level imports returns to its own start.

**1 deferred cycle** — the dependency is mutual, but at least one edge is an import inside a function body, so nothing breaks at import time. This is usually a deliberate workaround, and marks coupling worth revisiting rather than a live bug:

- `core.public_security` → `services.insights_profile` → `core.public_security`

### Coupling

Packages importing the most other packages (fan-out):

- `api.v1` → 10 packages
- `scripts` → 6 packages
- `services` → 5 packages
- `app` → 5 packages
- `ingestion` → 4 packages

Most-imported modules (fan-in) — the load-bearing ones, where a breaking change travels furthest:

- `db.session` — imported by 71 modules
- `db.base_class` — imported by 67 modules
- `db.models.user` — imported by 60 modules
- `api.deps` — imported by 39 modules
- `db.models.block` — imported by 29 modules
- `db.models.contractor` — imported by 22 modules
- `db.models.task` — imported by 20 modules
- `core.config` — imported by 20 modules
- `ingestion.db_connection` — imported by 19 modules
- `db.models.public_user` — imported by 19 modules
