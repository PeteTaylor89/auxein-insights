# Raise nginx's request-body cap above its 1MB default so photo/document
# uploads aren't rejected with a 413 at the proxy before reaching FastAPI.
# Kept below FastAPI's own MAX_FILE_SIZE (50MB in api/v1/files.py) so the
# app layer stays the authoritative size check.
client_max_body_size 25M;
