# Pinned to the versions the surfaces were developed and validated against.
#
# These are not casual pins. A climate archive is a time series, and an
# estimator that shifts underneath it leaves a step in the middle of the record
# that no downstream consumer can detect. rasterio also decides the LERC
# encoding every published COG is written in.
#
# Verified against the workstation venv that produced the 1986-2026 archive.
rasterio==1.5.0
scipy==1.18.0
numpy==2.2.5
pandas==2.2.3
pyproj==3.7.1
SQLAlchemy==2.0.40
psycopg2-binary==2.9.10
python-dotenv==1.1.0
boto3>=1.34,<2
pytz>=2024.1

# --- NOT used for computation. Pulled by the code that reaches the database. --
#
# THE IMAGE WOULD NOT HAVE STARTED WITHOUT THESE, and it would have failed the
# same way the GitHub workflow did for five days: every script here that touches
# the database goes `db.session` -> `core.config` -> `pydantic_settings`, and
# the list above names only what the interpolation MATHS imports. That is the
# whole bug class — a hand-curated dependency list that covers the algorithm and
# not the plumbing. Checked on 2026-08-30 by importing every script the
# entrypoint runs and diffing the result against this file.
#
# GeoAlchemy2 and shapely arrive through the ORM models, which carry Geometry
# columns this job never reads but does import.
#
# Loose-ish pins deliberately: unlike rasterio and numpy above, none of these
# touches a published value, so there is no archive to keep reproducible. They
# only have to be present and compatible.
pydantic==2.11.3
pydantic-settings==2.9.1
GeoAlchemy2==0.17.1
shapely==2.1.0

# Step 6 of the 18:00 pipeline. `cleanup_blacklist.py` imports
# `core.security.auth`, which decodes JWTs, so the daily job that expires
# revoked tokens needs the JWT library even though nothing here signs one.
#
# ADDED 2026-08-31, and only because the import audit was run inside the image
# before the schedule moved. This is the eleventh script in the chain and the
# ONLY one that failed; every other dependency the whole pipeline needs was
# already pinned above. It is precisely the failure this file's header warns
# about — a curated list that covers the maths and misses the plumbing — and it
# would have surfaced as one stage of six failing every night, on a job whose
# five other stages succeeded, which is the shape of thing this platform has
# repeatedly not noticed.
python-jose==3.4.0
